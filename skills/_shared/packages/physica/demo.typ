#import "@preview/physica:0.9.8": vectorbold, grad, div, curl, laplacian
#import "@preview/physica:0.9.8": bra, ket, braket, dd, dv, pdv, tensor, hbar

#set page(width: 170mm, height: 110mm, margin: 14mm)
#set text(size: 10pt)

= Scientific notation

#align(center)[
  $ vectorbold(v), quad grad f, quad div vectorbold(E), quad curl vectorbold(A) $

  $ laplacian phi, quad dd(x), quad dv(f, x), quad pdv(u, x, y, [1, 2]) $

  $ bra(psi), quad ket(phi), quad braket(psi, phi), quad tensor(T, -mu, +nu), quad hbar $
]
