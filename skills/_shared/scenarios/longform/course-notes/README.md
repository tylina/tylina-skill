# Course Notes

`notes.typ` owns the page system and semantic callouts. `main.typ` owns document identity and
chapter order; `chapters/session-01.typ` is one durable authored unit. Duplicate a chapter only
when the source material actually establishes another session or topic.

The prompts are not course facts. Replace them with supplied notes and preserve uncertainty when a
definition, example, source, or review answer is missing.
