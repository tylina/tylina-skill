# Report

`report.typ` owns reusable page and heading rules. `main.typ` owns front matter and section order;
`sections/findings.typ` demonstrates a stable authored unit. Add files only at durable boundaries,
and keep all page furniture in the template module.

The sample contains no real findings. Replace every prompt with supplied evidence, distinguish
decisions from proposals, and validate the full include graph after structural changes.
